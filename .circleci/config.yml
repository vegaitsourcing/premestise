version: 2
jobs: 
  build: 
    docker:
      - image: mcr.microsoft.com/dotnet/core/sdk:2.2
    steps:
      - checkout
      - run:
        name: Restore packages
        command:
          cd api && dotnet restore
      - run:
        name: Build app
        command:
          cd api && dotnet build
